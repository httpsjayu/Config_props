spring:
  boot:
    admin:
      client:
        url: http://localhost:1111/

  datasource:
    url: jdbc:mysql://localhost:3306/Safe_track
    username: root
    password: Jay@271203
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 8082
  
alert-service:
  messages:
    AlertSave: Alert Saved Successfully
    AlertSaveErr: Alert not saved
    AlertFetch: Alerts Fetch Successfully 
    AlertFetchErr: Alerts not Fetched
   

